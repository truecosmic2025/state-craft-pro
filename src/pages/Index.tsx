import { BookingSection } from "@/components/BookingSection";

const Index = () => {
  return (
    <div className="grain relative overflow-x-hidden w-full min-h-screen flex items-center justify-center">
      <BookingSection />
    </div>
  );
};

export default Index;
